spring:
    application:
        name: plus

elasticsearch:
    host: http://localhost:9200
    username: elastic
    password: changeme

management:
    endpoint:
        health:
            show-details: always